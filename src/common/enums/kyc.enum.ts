export enum KycType {
  NATIONAL_ID = "national_id",
  PASSPORT = "passport",
  DRIVER_LICENSE = "driver_license",
  OTHER = "other",
}

export enum KycStatus {
  SUBMITTED = "submitted",
  APPROVED = "approved",
  REJECTED = "rejected",
  EXPIRED = "expired",
}
